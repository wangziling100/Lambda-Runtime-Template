{
    "actions":[
        "cd app && yarn install",
        "cd app && yarn test",
        "cd app && rm -r test",
        "sam build",
        "sam local invoke \"SamDemoFunction\" -e events/event.json --env-vars env.json",
        "sam deploy"
    ]
}
